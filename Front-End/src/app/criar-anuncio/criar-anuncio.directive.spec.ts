import { CriarAnuncioDirective } from './criar-anuncio.directive';

describe('CriarAnuncioDirective', () => {
  it('should create an instance', () => {
    const directive = new CriarAnuncioDirective();
    expect(directive).toBeTruthy();
  });
});
